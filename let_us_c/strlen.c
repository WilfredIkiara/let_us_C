#include <stdio.h>

main(){
  char arr[25]= "castlevania"; 
  int len1, len2;
    
  len1 = strlen (arr);
  len2 = strlen ("humpty dumpty");

  printf("\nctring = %s length = %d",arr,len1);
  printf("\nsring = %s lenngth %d","humpty dumpty",len2);

}