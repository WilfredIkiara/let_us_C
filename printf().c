#include <stdio.h>
/*printf notes*/
main(){
  /*
  integer short signed         %d of %I
          short unsigned       %u
          long signed          %Id
          long unsigned        %Iu
          unsigned hexadecimal %x
          unsigned octal       %o
  real   float                 %f
         double                %If
  character   signed character %c
          unsigned character   %c
  string                       %s

dd digits specifying field
   decimal point separating field width frim precision(number of places after the decimal point)
dd digits specifying precision
   minus sign for left justifying the output in the specified field width
  */
}