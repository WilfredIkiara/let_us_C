#include <iostream>

int main(){
  double x = 3;
  double y = 4;
  double z;

  z = std::max(x, y);//returns the greatest of the nubers after comparison
  //min does the opposite
  std::cout<< z;
  //z = pow(2,3); calculates the power of the values entered 
  //z = sqrt(9) calculates the square root
  //z=abs();if you place a negartive number in the parenthesis of a absolute value it gives the positive version of the value
  //z=round(x);rounds 
  //z=call(); rounds up
  //z=floor(x); rounds down

  return 0;

}